const sum = (a, b) => {
  return a + b;
};

const mult = (x, y) => {
  return x * y;
};

// const rollDie=()=>{
//    return Math.floor(Math.random()*999)+1;
// }

// const rollDie = () => Math.floor(Math.random() * 999) + 1;

const rollDie=()=>(
     Math.floor(Math.random()*999)+1
)
